import 'package:flutter/material.dart';
import 'package:yemen_offers/core/constants/app_contants.dart';

class AppColors {
  static const Colors = Color(AppConstants.colorPrimary);
  static const Color primary = Color(AppConstants.colorPrimary);
  static Color lightText = Color(AppConstants.colorBlack);
  static const Color darkText = Color(AppConstants.colorWhite);
  static const Color text = Color(AppConstants.colorPrimary);
  static const Color background = Color(AppConstants.colorWhite);
  static const Color lightBackground = Color(AppConstants.colorWhite);
  static const Color darkBackground = Color(AppConstants.colorBlack);
  static const Color divider = Color(AppConstants.colorPrimary);
  static const Color error = Color(AppConstants.colorAccent);
}
